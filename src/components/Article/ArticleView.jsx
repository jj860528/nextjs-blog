import styled from "styled-components";

const ArticleViewEle = ({ className, article }) => {
  return <div className={className}></div>;
};

const ArticleView = styled(ArticleViewEle)``;
export default ArticleView;
